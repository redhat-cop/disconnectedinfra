rhel7:
  variables:
    username: !unsafe '{{ env `VMWARE_USER` }}'
    password: !unsafe '{{ env `VMWARE_PASSWORD` }}'
    vcenter_server: "{{ vsphere.vcenter_server }}"
    cluster: "{{ vsphere.cluster }}" 
    datacenter: "{{ vsphere.datacenter }}"
    datastore: "{{ vsphere.datastore }}"
    folder: "{{ vsphere.folder }}"
    network: "{{ vsphere.network }}"
    rhel7_vm_name: rhel7
    ddmmyy: "{{ lookup('pipe','date +%d%m%y') }}"
    rhel7_ssh_username: root
    rhel7_ssh_password: thisisabadpassword
    rhel7_iso_url: http://artifact.infrabuild.xyz/iso/rhel-server-7.8-x86_64-dvd.iso
    rhsm_username: rhsmusername
    rhsm_password: !unsafe '{{ env `RHSM_PASSWORD` }}'
    rhsm_pool_id: uuidforpoolid
    output_directory: "{{ artifact_directory }}"
  sensitive-variables:
  - password
  - rhsm_password
  builders:
  - type: vsphere-iso
    name: !unsafe '{{ user `rhel7_vm_name` }}'
    CPUs: 2
    RAM: 4096
    RAM_reserve_all: true
    username: !unsafe '{{ user `username` }}'
    password: !unsafe '{{ user `password` }}'
    vcenter_server: !unsafe '{{ user `vcenter_server` }}'
    cluster: !unsafe '{{ user `cluster` }}'
    datacenter: !unsafe '{{ user `datacenter` }}'
    datastore: !unsafe '{{ user `datastore` }}'
    folder: !unsafe '{{ user `folder` }}'
    disk_controller_type: pvscsi
    firmware: bios
    floppy_files:
    - ks.cfg
    boot_wait: 10s
    boot_order: disk,cdrom,floppy
    boot_command:
    - <esc><wait>
    - linux ks=hd:fd0:/ks.cfg<enter>
    guest_os_type: centos7_64guest
    insecure_connection: "true"
    iso_checksum: none
    iso_urls:
    - !unsafe '{{ user `rhel7_iso_url` }}'
    network_adapters:
      - network: !unsafe '{{ user `network` }}'
        mac_address: "{{ '00:50:56:A8' | random_mac }}"
        network_card: vmxnet3
    vm_name: !unsafe '{{ user `rhel7_vm_name` }}'
    ssh_username: !unsafe '{{ user `rhel7_ssh_username` }}'
    ssh_password: !unsafe '{{ user `rhel7_ssh_password` }}'
    notes: !unsafe 'SSH Username {{ user `rhel7_ssh_username` }}\nSSH Password {{ user `rhel7_ssh_password` }}'
    storage:
      - disk_size: 40906
        disk_thin_provisioned: true
    export:
      force: true
      output_directory: !unsafe '{{ user `output_directory` }}'
      name: !unsafe '{{ user `rhel7_vm_name` }}-{{ user `ddmmyy` }}'
  provisioners:
  - type: ansible
    playbook_file: playbook.yml
    extra_arguments:
    - --extra-vars
    - !unsafe 'rhsm_username={{ user `rhsm_username` }} rhsm_password={{ user `rhsm_password` }} rhsm_pool_id={{ user `rhsm_pool_id` }}'
    only:
    - !unsafe '{{ user `rhel7_vm_name` }}'
